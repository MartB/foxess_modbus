homeassistant==2026.9.3

# Testing
pytest-homeassistant-custom-component==0.13.366  # Matching version
psutil-home-assistant  # Not sure why this is needed?
fnv_hash_fast  # Or this?
pytest-asyncio

# For pytest. Keep in sync with the requirement in manifest.json. The tmodbus extra brings tmodbus and
# serialx; Home Assistant ships this itself from 2026.9.0, so the range is what core pins, not a hard pin.
modbus-connection[tmodbus]>=4.10,<5

# Tooling
colorlog==6.10.1
pre-commit==4.6.0
ruff==0.9.4
mypy==1.15.0

# Typing
homeassistant-stubs==2026.9.3 # Matching HA version
types-python-slugify==8.0.2.20240310
voluptuous-stubs==0.1.1
